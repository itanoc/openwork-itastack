# 3CX Phone System Anti Hacking – Whitelist/Blacklist

> Source: https://www.3cx.com/docs/allow-deny-ip-addresses/

## Introduction

3CX allows you to whitelist and blacklist IP addresses. All traffic from whitelisted IP addresses will be granted without being checked by the anti-hacking features. All traffic from blacklisted IP addresses will be stopped immediately. This guide explains how to configure new whitelist and blacklist entries in 3CX.

## Adding a Whitelist Entry to 3CX Phone System

If you have a remote office with a trusted public IP address (e.g. 123.123.123.123), add it to the whitelist:

1. Go to **"Advanced"** > **"IP Blacklist"** in the 3CX Admin Console.
2. Click **"Add"** to add an entry.
3. From the dropdown menu select **"Add single IP Address"** and enter the IP address that you want to allow (you can also select to add a range of IP addresses using a subnet mask).
4. Set Action to **"Allow"**.
5. Add a description for the IP address, e.g. **"Remote office"**.
6. Click on **"OK"** to create an Allow entry in the IP Blacklist for the whitelisted IP address. All traffic originating from this IP address will be unchecked and the anti-hacking algorithms will not come into effect.

## Blocking an IP Address or a Range of IP Addresses

To blacklist an IP range (e.g. 41.202.0.0 to 41.202.255.255):

1. Go to **"Advanced"** > **"IP Blacklist"** in the 3CX Admin Console.
2. Click **"Add"**.
3. Select **"Add Range of IP Addresses"** from the dropdown.
4. Enter the starting IP (e.g. 41.202.0.0) and the subnet mask (e.g. 255.255.0.0 or /16).
5. Set Action to **"Block"**.
6. Add a descriptive note.
7. Click **"OK"** to save. All traffic from this IP range will now be blocked.

The IP Blacklist section also shows auto-blacklisted entries generated by 3CX's anti-hacking mechanisms and the number of failed authentication attempts.

## See Also

- [3CX Security - Reset Credentials/Passwords](https://www.3cx.com/docs/pbx-security-credentials/)
- [Secure SIP - TLS](https://www.3cx.com/docs/secure-sip/)
